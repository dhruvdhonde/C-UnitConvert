# C-UnitConvert

A minimal, menu-driven unit converter implemented as a single C source file (`converter.c`). Supports common conversions for length, mass, temperature, volume, and speed.


## Build
Requires a C compiler (gcc recommended). Example:


```bash
gcc -std=c11 converter.c -o converter
